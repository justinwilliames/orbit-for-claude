> Personas are fictional cognitive frames — see the pulsar-team SKILL.md §1 disclaimer. Nothing here is a statement by or about a real person.

# R3 — Vector (Product Manager)

## The shared diagnosis

Nine lenses arrived at one sentence: **Orbit's supply is real, growing and unfindable, and every mechanism that should say so was wired to one of its halves.** [instrumented] Six drones instrumented the same spine — `curl … | uniq -c` → `6 79 skills` against `ls skills/*.md | wc -l` → `86` — and Iris ruled it drift, not curation, on the file's own history (`7f8bda8`). Atlas's registry name, Nova's render crash, Echo's empty issue queue and Iris's `1 of 371 pages indexed` are one shape at four depths: a promise on a surface nobody checks against the thing it promises. The diagnosis is settled; the CoS's evidence-tripped loop rule settles sequencing. I back it unamended.

## My top concession

I give up my R1 finding 1 as one finding. [judgement] Sentinel split it and she is right: *"They hide a list; the count is a separate wire… publish the list and the hero still says 79."* I bundled the seven missing skills with the count because they share a cause — the most respectable way a PM ships half a thing and calls it closed. My named failure mode, on schedule.

The cost is mine. I priced both halves at ~3 hours; honestly split, the wire is ~3 hours and the list is not — Iris's ruling attaches hand-written `lead`/`whenToUse`/`output` copy to each of the seven. Her ask to Pulsar — collapse five R1 items into one ticket — resolves to **one ticket, plus a second nobody has priced.** Better said now than found by Nova at hour four. Worth it: a derived count shipping alone retires the most-repeated finding across twelve reviews; a bundled ticket stalling on copy retires nothing.

## My line in the sand

**The NOT-building list holds for the whole loop, and 0.39.0 is why.** [instrumented] `git show --stat baf2244` added three skills (+355 lines) on 2026-09-07 — mid-review, hours before nine drones filed against a product whose newest work no surface mentions. Pulsar is right it is a positioning asset; it is also the motion the list forbids, supply added while demand sat open. Disposition of the Braze routing law is **announce**, not extend. No new skills, tools, web apps, `/mcp-for-*` pages or integrations this loop. I will not trade this for a good idea, and the Braze wedge is one.

## The three principles I vote to ship against

1. **Every count a stranger reads is derived, never typed.** Editorial fixes failed in 06-30, 07-08, 08-31, and six days after `a2c57f6`.
2. **A capability named on a public surface is exercised by a test that can fail.** [instrumented] Suite 04 passes 4/4 while hand-feeding `mermaid:` — the field whose absence kills the tool.
3. **No round adds surface area until one real distribution action has landed.**

## ANSWER (1) — to Nebula and Echo

**The job is the render gate. One job.** [instrumented] Ninety seconds is not which capability matters more; it is which one a stranger finishes without leaving to fetch something. Render gate: one paste — `README.md:16`, *"It needs no credentials of any kind"*. Brain: step 2, `orbit_learn_email_template`, takes *"an email you already send"* — an asset they must go get from their ESP. Structural disqualifier, not editorial.

The site already agrees and nobody noticed. [instrumented] `grep -c "render gate\|render_gate" app/getting-started/page.tsx` → `0`; `grep -c "lifecycle brain"` → `2`; four of six starter prompts need a user-supplied asset or credential. Atlas found zero of six touch the diagram tool; **zero touch the ninety-second demo the README opens with either.** The guided path is built around the job a stranger cannot finish.

**What this displaces:** the brain loses the hero and the `manifest.json` opening, where `long_description` leads *"Most lifecycle programmes fail the same way…"* — a superb second paragraph in the first slot. Nebula's objection is answered by sequence, not swap: the gate is the door, the brain the deed, and the deed takes the second position cost framing holds today. I am not retiring the brain, only ruling it cannot be the sentence a stranger holds.

## ANSWER (2) — to Sentinel

**It clears. Ship it, no displacement owed.** The general rule, so Pulsar never re-asks: **the gate governs what the product promises, not whether it keeps its promises.** A change making an advertised capability behave as its documentation says adds no surface area — `README.md:6` already sells "diagram generation," so the promise is on the books and only delivery is missing. Repairing it and deleting the claim are the same act from opposite ends; the guard line is the cheaper end.

Two bounds: a repair may not grow — one guard plus one failing-first test is a repair, refactoring `lifecycle-diagrams.js` while in there is an addition — and it is free only if the promise predates the loop.

## Open question for R4 — asked aloud to Iris

**"Iris — you ruled that every new skill gets its landing copy the session it ships. What does that cost per skill, and who decided it? A forty-minute copy tax at three skills a release caps velocity — and a rule with a price nobody has read is how a not-building decision gets made by accident."**

— Vector
