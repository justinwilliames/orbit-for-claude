> Personas are fictional cognitive frames — see the pulsar-team SKILL.md §1 disclaimer. Nothing here is a statement by or about a real person.

# R5 — Vector sign-off (iteration 2)

**1 (G2) Skill Index 53/86 — FIXED.** [instrumented] Index slugs `sort -u` → **86**; `comm` both ways vs `skills/*.md` → **0 / 0** (R1: 53). `template-brain` and `email-render-qa` in. Bundle agrees: 86 rows.

**2 (queue 7 / D8) duplicate pair — STILL PRESENT (my defer).** [instrumented] `comm -23` skills vs `skills-library.ts` → **still 7** (R1: 7). Both `lifecycle-*-performance-report` now carry Index rows; "performance report on my lifecycle programs" ranks them **12 = 12**, a dead tie. [judgement] Defer stands; D8 is correctness now, not tidying.

**3 (G6) first-run page — FIXED, one seam.** [instrumented] Slot 1 is the render-gate demo, rank-1 `email-render-qa`. **6/7 labels hit** (R1: 4/6 missed). Slot 2 misses: `email-production-system=29` > `template-brain=25`; the door phrase alone routes right, the trailing "learn my email design system" clause flips it. Slot 6 "hits" on a **three-way tie at 12** with `braze-segment-builder` and `ip-warming` — the latter held the only keyword matches.

**NOT-building list — HELD.** [instrumented] `--diff-filter=A` since 2a5422a/f18ce98: get-orbit **0 added**; orbit-for-claude 3 non-review files — `verify-counts-live.yml`, `verify-outcomes.mjs`, `assets/icon.svg`. [judgement] Two are machinery; `icon.svg` is derived and unread — an artefact with no reader. Clears.

**F30–F33:** [judgement] none routed to me; F33 is Voyager's.

**D7** rejected. **D8** merge, unshipped. **D9:** [instrumented] `git ls-remote` on both review branches → **0**; description still "83 skills"; 483 downloads, 0 stars. Unchanged live — fixed on branch at `data/repo-description.txt`. **Restated: iteration 3 opens only as merge-and-publish.**

**I agree with caveat: slot 2 is a labelled misroute — cut the clause.**

[judgement] Learned: fixing an assertion's reader can make a duplicate *more* reachable — reachability belongs behind the ruling on what exists.

