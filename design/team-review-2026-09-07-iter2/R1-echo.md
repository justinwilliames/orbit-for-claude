> Personas are fictional cognitive frames — see the pulsar-team SKILL.md §1 disclaimer. Nothing here is a statement by or about a real person.

**Verdict:** three of four doors carry the anecdote; the fourth — README:3, the door with the most organic reach — doesn't, and my own R5 said otherwise. The manifest description passes the friend-repeatable test at a B+: the hook and the cost line do the work, the middle sentence is skippable weight nobody retells. Distribution is exactly where iteration 1 left it — nothing moved, nothing should have.

**Top 3 findings**

1. **[instrumented] The four doors are NOT uniform — README:3 is missing the sentence, contradicting my own R5 claim.** `sed -n '3p' README.md` → *"It checks every email in a real browser before you send, and turns the emails you already send into a design system."* Compare `orbit.md:43`, `manifest.json`'s description (source and packed, `unzip -p *.mcpb manifest.json`), and `get-orbit/app/page.tsx:182` — all three carry, verbatim: *"...the first thing it ever caught was its own brand colour, too faint to read."* README has neither the anecdote nor the payoff. On F11/F12's territory but NEW: my own R5 sign-off asserted "same sentence now opens all three doors (README:3, packed manifest, hero subhead)" — untrue as literal text even then, and now that orbit.md is a confirmed fourth match, README is the visible outlier. It's also the highest-reach of the four: what a search hit, a link preview, or a pasted repo URL renders. The strongest asset in the bundle is missing from the door most likely read cold.

2. **[judgement] Friend-repeatable grade on the manifest description, the install-decision text: B+.** Sentence 1 (the anecdote) is an A — concrete, self-deprecating, falsifiable, the kind of line a stranger retells without meaning to. Sentence 4 (free, no key, no payment) is an A — it answers the one objection that blocks a click. Sentences 2–3 (undefined "lifecycle brain," then an 86/135/ESP-name spec dump) are the weak middle a retelling drops. Order is right — deed → mechanism → capability → cost — so this isn't a rewrite demand. One risk I couldn't instrument: the field is 429 characters (74 words), and the manifest carries a separate 1,652-char `long_description`, so `description` is almost certainly the short card-view string. If Desktop's extension browse card truncates under ~160 characters — a normal marketplace pattern, no screenshot available here — the cut lands mid-sentence-1, before "too faint to read." Flagged, not filed as confirmed.

3. **[instrumented] Distribution unchanged, as expected — a checkpoint, not a finding.** `gh repo view --json stargazerCount,forkCount` → `0`/`0` (R1 and R5: same). `gh api repos/.../releases --paginate` → 31 releases, 483 total downloads (R5: 483 — flat, consistent with nothing merged to main). `gh repo view --json description` → still literal `"83 skills and 135 tools"`, unchanged live — tracked as D6/Pulsar's caveat, owner Justin. Reporting and moving on, per brief: not mine to re-litigate, but it means a stranger meets a stale count on GitHub before ever reaching README:3's weaker sentence.

**The single thing I'd ship:** port the anecdote sentence from `orbit.md:43` into README:3, replacing the current paraphrase. Under 15 minutes — one string, one tracked markdown file, no build or redeploy dependency (README renders straight from the repo, unlike the site or the packed bundle). Fully reversible — one `git revert`. Owner: Nebula for the copy, Echo to verify byte-match the way I did the other three. This is a copy-consistency fix on a surface that already gets organic traffic — distinct from Iris's channel-acquisition levers (Search Console, Reddit, awesome-mcp, directory listing), which put a stranger in front of the door. This fixes what's printed once they open it.

**What I'd defer:** rewriting the manifest description's middle sentences until someone actually screenshots the Desktop extension browse card — don't cut copy against a truncation point nobody has confirmed exists.

**Question, aloud, to Atlas:** "Atlas — have you got eyes on the Desktop extension directory's card view? I need to know whether `description` truncates before character ~160, because if it does, the anecdote's payoff never reaches a stranger scrolling the list, and that changes how I'd resequence the manifest text."

**CHALLENGE (to Justin):** the R5 tally called the three-door resequence "shipped, as proposed" on three doors matching. It isn't proposed-complete with a fourth door open and diverging — worth five minutes before calling this thread closed, not a blocker.

— Echo
