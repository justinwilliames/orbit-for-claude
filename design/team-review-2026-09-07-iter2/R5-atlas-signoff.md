> Personas are fictional cognitive frames — see the pulsar-team SKILL.md §1 disclaimer. Nothing here is a statement by or about a real person.

**R1#1 (G3) — README undersells the gate: FIXED.** [instrumented] `sed -n 4p README.md`: "The GitHub release below is ungated. The website download asks for a free account — email, first name, a password and a security question, no card." Replaces "one email"; matches `/downloads:77`.

**R1#2 (G3/G20) — reset dead-ends on lockout: FIXED.** [instrumented] `grep -n too_many_attempts app/account/reset/page.tsx` (get-orbit): a distinct branch (line 88) with a lockout message plus a `/contact` link (line 105) — no longer folds into "Something went wrong."

**R1#3 (G3) — gate's reason has no engine: STILL PRESENT, by ruling.** [instrumented] `/downloads:77` still says "so I can reach you"; `email-reset/route.ts:28`/`account/email/page.tsx:227` confirm no transactional-email system. R4 deferred as D7 pending D2 — price honest now, payoff still unbacked.

**Priced Stripo copy fix: STILL PRESENT.** [instrumented] `stripo-emails.js:258,278` unchanged — still "set ORBIT_STRIPO_REST_API_TOKEN" vs. the working `stripo-onboarding.js:470` pattern. Unclaimed, fourth cycle.

**Hero subhead (queue item 9): closed, restated.** [instrumented] Prior render stands: 10.00 lines at 375px, CTA at 612–652 of 812. No trim required.

No F30–F33 row routed to me this round.

**Re-walked the first three minutes** (README → `/downloads` → packed manifest → getting-started prompt #1): [instrumented] the honest six-field count holds at both doors, the anecdote appears in README:3 and the unzipped `.mcpb` manifest verbatim, and prompt #1 is the render-gate demo needing nothing from the visitor — no HTML, no key, no brand kit. A stranger does the deed cold.

I agree with caveat: `/downloads:77`'s reach claim still isn't backed by a send pipeline — fix it before a third cycle passes.

I learned a door can be honest about price before it's honest about payoff — two fixes, two owners.

— Atlas
