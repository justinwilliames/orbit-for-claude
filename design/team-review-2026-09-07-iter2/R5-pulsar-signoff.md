> Personas are fictional cognitive frames — see the pulsar-team SKILL.md §1 disclaimer. Nothing here is a statement by or about a real person.

# R5 — Pulsar (iteration 2)

**G15 — rows died silent: FIXED.** [instrumented] R1: `grep R5-*.md` → 0/9 dispositions. Now `verify-outcomes.mjs` (d53fced): shas `2a5422a/3ea16ea/d53fced/f18ce98` **ok**, then "41 of 45 checks failed … no closing-round file exists yet". Presence-only; dead unless re-run after R5 lands.

**Cost unpriced: STILL PRESENT.** [instrumented] R1: wave 4 unpriced. Iteration-2 RUN.md quotes one figure, "1,600,742 tok"; R1's 1,428,830 (wf_4d10337b-b53) and eleven direct spawns carry ids only. Build wave unpriced again.

**G16 — 09-11 bar: FIXED by ruling, unchanged live.** [instrumented] R1: v0.33.0 = 16, total 483. Re-run: 16, 483 — unmoved. D5-amended retires it.

**F30–F33.** [instrumented] No `R5-*.md` existed at my write time (`no matches found`): F30–F33 uncollected; none routed to me; F33 is Voyager's.

**Ledger.** [instrumented] Items 1–6 land in d53fced (1,2,3,6) / 3ea16ea (4,5); G1 in 68b3933. `verify:counts` → "All 21 checks pass"; bundle `orbit.md` md5 `db37120b…` = HEAD; `assets/icon.svg` ships; `.mcpbignore` gone; `build-mcpb.yml:105` "Verify the tree that ships".

**Conditions.** [judgement] 2 and 4 met; 3 gated on the re-run; 5 does **not** fire — item 4 is a build with a named displacement, so my R1 halt call was wrong; 6 unmet. **Iteration 3 opens only as merge-and-publish** (D9).

**D1–D9.** [instrumented] All unanswered; no commit past d53fced; `gh repo view` still "83 skills" (fixed on branch at data/repo-description.txt). A day costs an unindexed domain and ~1.5M tokens correcting an unreachable branch.

**Tripwires.** [instrumented] `/support` **200**; bar retired; Search Console **140 days**, 7 unchecked, 0 checked.

I agree with caveat: condition 6 is unmet, so iteration 3 is merge-and-publish or halted.

[judgement] A gate that only detects stays unwitnessed until someone runs it where it could fail.

— Pulsar
