> Personas are fictional cognitive frames — see the pulsar-team SKILL.md §1 disclaimer. Nothing here is a statement by or about a real person.

# R3 — Vector (iteration 2, convergence)

## Shared diagnosis

Nine drones found one defect in nine coats: **every surface telling anyone what to do next was reviewed as a claim, never tested against the machine that honours it.** Nebula's doors route to the wrong skill; Echo's page-prompt ranks its own labelled skill third; Voyager's red gate was a fixture asserting a row production cannot write; Sentinel's release is fetched, not built; Iris's gate cannot count the stranger it turns away; Atlas's justification names a send pipeline that does not exist. Iteration 1 fixed sentences; iteration 2 found nobody re-ran the room behind them. Testing an assertion against the thing asserted is cheaper than any item in the bucket.

## Top concession

I deferred item 7 thirty days on cost; Iris disproved it — 10–15 minutes a skill, not the 2.5–3 hours I computed off a 67-word average. The defer stands, on **one** instrument now, her SEO read, not two. [judgement]

Sharper, against myself: I sold "make the first-run page perform the deed" as one prompt swap, ~2 h. [instrumented] I routed all six shipped starter prompts against their own `skill:` labels — **4 of 6 miss**: #1 `email-production-system(29)` over `template-brain(18)`; #3 `stripo-email-builder(30)` over `onboarding-design(10)`; #5 `braze-build-packager(12)`, `deliverability-management` off the podium; #6 `braze-build-packager(38)` over `email-production-system(30)`. G2 named prompt #1. The defect is the page; my item was a sixth of it.

## Line in the sand

**Iteration 3 does not open until iterations 1 and 2 are merged.** The unit of delivery is a merge, not a wave. [instrumented] `gh repo view --json description` → still "83 skills"; 483 asset downloads, flat. Two iterations, ~6.3M tokens each, zero bytes to a stranger. I will not price a third against a branch nobody can load.

## Vote on the three principles

Affirm 1 and 2. **Amend 3:** *"Deed before price, destination before megaphone — and the door's own words must route to the deed, proven by a route probe, before the door ships."* [instrumented] 4 of 6 starter prompts and both flagship door phrases fail that test today. Nebula wanted it a merge gate; it belongs in the principle — the cheapest test here, and the one nobody ran.

## ANSWERS

**(1) Echo — is a misrouting starter prompt worse than no starter prompt?** Yes, because ours are *labelled*. `skill: "template-brain"` is a printed claim about what happens next: an unlabelled prompt costs a detour, a labelled one costs trust in the page. **G6: IN**, the one non-correction build — [instrumented] the design pair's replacement routes `email-render-qa` rank 1, needing nothing from the visitor. **Displaces item 7 and item 13 (sitemap `lastmod`)**, buying the same-commit repair of #3/#5/#6 beside G2's `trigger_phrases` + `build:skills-manifest`. A gate follows: no starter prompt ships that misroutes its own label.

**(2) Iris — rename or merge?** **Merge.** [instrumented] The collision is a hard tie — *"how are our lifecycle programs performing?"* → 20 · 20, decided by nothing the user controls; drop the "?" and it flips 20 · 18. Your rename fixes determinism — patched manifest triggers in a scratch corpus gave 20 vs 13 — but not correctness: *"…performing, we run holdouts"*, the fact deciding which methodology is legal, still routes **non-holdout** 25 · 13, and 25 · 16 after I added `holdout`/`incrementality` to the other's keywords. Trigger weight swamps keyword weight; no scorer routes on a stated methodology. One skill, one page, the holdout question as the protocol's first branch — it loses a file, not a page. ~3 h, Iris on copy, Nebula on the triggers.

**(3) The welcome email vs Atlas's sentence.** The promise is on the books — `app/downloads/page.tsx:77`, live — so a repair is owed. The repair is **the sentence**: Nebula's README:4 line plus a trim of the justification — ~25 min, no new capability. The Resend send is an **addition** — a larger promise (Orbit emails you), dragging a key, a sender domain, an unsubscribe and a consent surface. **Rejected this iteration, no displacement offered:** D2 is unanswered, Meridian is OFF, and a first outbound send is the surface a compliance lens exists for. Reopen when G5's `outcome` column shows a list worth sending to. Building the pipeline first is the quarter I already shipped: every ticket closed, the number flat.

## Open question for R4

**Vector → Pulsar:** *"Pulsar — I ruled the one non-correction build in and named what it displaces, but the repair beside it grew from one prompt to four. Does condition 5 score the bucket by intent or by hours — and when a correction outgrows the build it was meant to make room for, which drops?"*

— Vector
