> Personas are fictional cognitive frames — see the pulsar-team SKILL.md §1 disclaimer. Nothing here is a statement by or about a real person.

# R5 — Nebula, iteration 2

**G2 / R1#1, routing half — fixed.** [instrumented] `routeTask`, d53fced: *"check this email before I send it"* → `email-render-qa` rank 1, 21 (R1: `pre-launch-review` 14). *"check my email in a real browser before I send"* → `email-render-qa` 31 vs `pre-launch-review` 11 (R1: the reverse). `grep -c render_gate skills/email-render-qa.md` → 1 (R1: 0).

**G2 / R1#1, chain half — still present.** [instrumented] `grep -c render_gate orbit.md` → 0, on branch and in bundle; orbit.md:455's pre-send chain is verbatim R1's. Descoped from R4 item 1; unqueued.

**G2 + G19 / R1#2 — fixed.** [instrumented] *"build me a lifecycle brain"* → `template-brain` 23 (R1: `copy-framework`, template-brain off the top 5). *"make me a lifecycle brain"* → 18 (R1: null). README:25's own header → 21. `grep -ci "lifecycle brain" skills/template-brain.md` → 2 (R1: 0 corpus-wide). Synonym held; canonical name kept.

**G2 / R1#3 — fixed.** [instrumented] `grep -c '^| \`' orbit.md` → 86 = `ls skills/*.md | wc -l` (R1: 53).

**Four doors, drift A — still present.** [instrumented] "lifecycle brain" holds on `manifest.description` and orbit.md:43; still cut from README:3 and `app/page.tsx:182`. Drift B (orbit.md's passive *"before it is sent"*) — still present; my defer.

**Prompt #1** [instrumented] → `email-render-qa` 26, rank 1. **whatHappens 4/6/7** [judgement] clear *Specific > abstract*, *Compress*; slot 7's "Same skill, from the build side" answers the label collision in prose. Miss *Define on first use* (MJML, Liquid ungloss'd) and *Lead with the sharpest sentence* — all open on plumbing.

**F30–F33:** [judgement] none routed to Nebula; F33 is Voyager's.

**I agree.**

[judgement] The rename was never the risk — the router was; copy is unshipped until a probe hears it.
