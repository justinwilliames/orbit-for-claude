> Personas are fictional cognitive frames — see the pulsar-team SKILL.md §1 disclaimer. Nothing here is a statement by or about a real person.

# R5 — Nebula · sign-off

Opened at 900px: `orbit-render-gate-html.data.html`,
`orbit-inbox-preview-html.data.html`, `orbit-rfm-map-html.data.html`,
`orbit-send-calendar-html.data.html`. **Not on :8900** — that dir holds only
`esp-matrix.data`; I rendered and served my own. Flagging it: the brief asserted
files I had to make.

## Disposition

**R1-1 — signature move scored zero. FIXED, and it propagated.** Nova put the
move where I never asked: send-calendar's tag density reads *"Proxy for audience
collision only — see caveats"*; *"Braze returned no parseable send time… the
header count includes them; the grid cannot."* Orbit grading its own instrument,
unprompted.

**R1-2 — voice splits. FIXED** for these four.

**R1-3 — a dozen interchangeable. STILL PRESENT**, untouched, correctly deferred.

**Nova's three deviations — all right, all against me.** Three panes, not four:
the widget measures *widths*, and the caption downgrades them to assumptions
while keeping the cut as observed. "Four clients" was a fabricated capability.
Same for 11px — carried by a measured 2.81:1 at 11px, not my invented iOS
auto-scale. Same for 6-of-10: my "41k segment in 4 minutes" needed membership
data the tool has not got. **The governing rule landed: 172px overflow, 2.81:1,
22:40 inside quiet hours, rfm labels colliding on screen.** One quibble — 172px
is a WARN, and it is the stomach-drop.

**esp-matrix — shippable.** 0.32.0 stands verbatim at line 252, superseded not
rewritten, by *"That note has stood for eleven releases and it is retired here."*
Supersession beats deletion. Condition met.

**Roster — both audiences: three fixtures, two captions.** render-gate and
inbox-preview read cold; rfm-map does not. No fourth.

**I agree.**

Learned: I briefed mechanisms I could not measure, and the build was more honest than my brief.

— Nebula
