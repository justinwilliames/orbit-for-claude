> Personas are fictional cognitive frames — see the pulsar-team SKILL.md §1 disclaimer. Nothing here is a statement by or about a real person.

# R5 — Echo sign-off

Files opened: `R5-WHAT-CHANGED.md`, `R1-echo.md`, `R3-echo.md`, `orbit-render-gate-html.html`, `orbit-client-matrix-html.html`, `orbit-esp-matrix-html.data.html` (all served, 900×520).

**1. Justin's ruling — recorded, not relitigated.** Both audiences: lifecycle marketers powering up Claude AND lifecycle newcomers (startups). Costs my roster honestly: I built "three, homepage only" by parking `cohort-retention` and `esp-matrix` onto a separate "technical evaluator" page that assumed one visitor. With both audiences on the same homepage sequence, that parking is void — my roster is under-scoped for the practitioner half. Not re-litigating the count; flagging a fresh pass is owed.

**2. Outcome-subhead-card — re-run.** Render-gate: *"Waiting for HTML to gate — run orbit_render_gate on an email to measure it in a real browser at 640px and 390px."* Client-matrix: *"Waiting for a client simulation — run orbit_client_sim to see how each email client degrades this HTML."* Both now render in the primary stage — the visibility bug is genuinely fixed. Neither gained a bold outcome-first headline card; both still open with a tool-name-and-mechanism sentence, no `rfm-map`-style "what changes" line. **The fix reached the neighbor (Atlas's data-ready wiring) — my finding is still present, confirmed live on both.**

**3. Esp-matrix kill — survives.** Content unchanged: *"Braze · Bearer REST API key · Liquid,"* *"SFMC · OAuth2 client-credentials (token lifetime 20 min) · AMPscript."* Never audience-contingent — auth-header plumbing fails a non-technical marketer and a non-technical newcomer alike. The ruling doesn't rescue it. Kill stands, firmer ground.

**Verdict:** I agree, caveat — roster needs a re-scope pass for the practitioner half before R5 closes.

**Learned:** a heuristic scoped to one visitor doesn't survive a ruling that adds a second — check audience binding before re-running the instrument.

— Echo
