> Personas are fictional cognitive frames — see the pulsar-team SKILL.md §1 disclaimer. Nothing here is a statement by or about a real person.

# R2 — Nebula × Echo · story cross-reference on 23 widgets

## Where we agree

**Nebula:** Twenty-three does not ship.
**Echo:** And the blocker isn't data. We both walked in holding "empty = unsellable" and came out with the same object: the empty state is a *chassis*, inconsistent in a way no fixture fixes.

## The finding needing both lenses `[instrumented]`

All 23 empty states parsed from served markup; `review-gallery`, `design-system`, `audit-report` and `esp-matrix.data` opened at 900×520. We each had half and both rounded wrong:

- **Full outcome card** (`h2` + `.card-note` mechanism): **3** — `ab-readout`, `rfm-map`, `send-calendar`.
- **`h2` label, no mechanism: 16** — incl. `cohort-retention`, `design-system`, `esp-matrix`.
- **Nothing above the void: 4** — `audit-report`, `qa-report`, `review-gallery`, `state-matrix`.

Separately: **7 of 23 carry zero `.o-card` in static markup** — the four above plus `client-matrix`, `lifecycle-flow`, `render-gate`. Atlas found three. It is seven. And **12 bake a static "Waiting for…" sentence; only 10 name the tool**.

**Echo:** My mechanism holds, my roster was 3-for-4. `cohort-retention` is *not* cardless — two `.o-card`s, zero notes, so it sits in the sixteen. I missed five others by inferring twelve widgets rather than opening them. The cost of my 11/23. `[sharpened]`

**Nebula:** And I retract "fifteen of twenty-three are the identical object." Instrumented: twelve with the sentence, ten with the tool name. The flattening was the error, not the arithmetic — three tiers, not one shelf. `[retracted]`

## Where we fight

**Echo:** Nebula, your hero is in the bare seven. `render-gate` has zero cards and zero static empty-state copy. You want to lead the site on the worst first impression in the set.

**Nebula:** *Delighted* you found it, and it doesn't move me. A populated `render-gate` needs no card — the findings ARE the copy. *"BLOCKER · Hero is 812px in a 640px shell — 172px of the CTA sits off-canvas"* is the outcome line. The card only matters when nothing sits behind it.

## THE NUMBER

**Nebula — I concede five, partially.** Five was an inventory answer in a taste answer's coat; Vector was right. But I refuse one. My thesis is *recurrence* — one company, one week, one email, read by different instruments. One screenshot cannot demonstrate recurrence, only a screenshot. It optimises for the fold, and the fold is not the argument.

**Echo — my number is two.** The friend-repeatable test needs two artefacts: one that makes a stranger feel the stakes, one that makes them say what changes. `render-gate` FAIL does the first — "172px of your button is off-screen" repeats at a pub. `rfm-map` does the second — "610 people carry $198k and nobody's emailed them in 90 days." Two is the smallest set carrying both jobs; three is where surface area creeps back.

**Joint recommendation: 3 — `render-gate` (FAIL), `rfm-map`, `inbox-preview`.**

**Nebula:** Three because two can't show recurrence. You need a third reading of the *same send* before "one email, many instruments" reads as design rather than coincidence. `inbox-preview` is that third — it reads the subject line the gate just failed.
**Echo:** Taken. Note we reach Iris's number by a different route and roster — she picked `esp-matrix` (the broken one) and `lifecycle-flow` (bare seven). The agreement is worth less than it looks.

## The story fork — our decision, needing Iris's ratification

**ONE email walked through the whole gate.** Not five jobs.

**Nebula:** Five jobs means five invented worlds and five times the fixture cost for the same three slots. One send composes: Merrowfield's *"Winter Restock — the Ember candle is back"* feeds `render-gate`, `inbox-preview`, `dark-pairs`, `preheader-clip` and `send-calendar` from one artefact.
**Echo:** One caveat that changes the bible: `rfm-map` and `cohort-retention` are *list*-level — one email cannot produce them. So it's one email **plus the list behind it**: one company, one week, one send, one list. Say it out loud or the fixture set fails at widget four.

**FLAGGED: needs Iris's ratification before 18 fixtures are cut.** She owns it; we unblock, not overrule.

## `review-gallery` — settled by opening it

At 900×520: `#rail-list`'s `innerHTML` → `""`. `.rail` measures **300×482px** — a third of the widget's width, blank white. `#rail-count`, `#head-name`, `#head-meta` all `""`. But the stage reads **"Waiting for creatives…"**, and static markup carries Desktop/Mobile, Approve / Needs changes / Pending, "Copy review", "Send review to Claude" and a notes placeholder.

**Verdict: Atlas is right about the third, Echo about the two-thirds.** Neither is a full account. `[instrumented]`

**Nebula:** I retract "no footer bar" — it renders *"Made with Orbit AI — a free lifecycle marketer, built into Claude. By Justin Williames."* like every sibling. I called a different chassis off a screenshot I read too fast. `[retracted]`
**Echo:** "Passes cold" stands for the main column, not the page. A 300px void beside a working control panel is a pass with a hole in it — downgraded from proof point to cheap fix. `[sharpened]`

## Other sharpened findings

- **Nebula, sharpened:** `audit-report` doesn't merely demote its action to a ghost — it has **no "Send to Claude" at all**, only a white "Copy findings". Every sibling ships both. `[instrumented]`
- **Echo, confirmed by screenshot:** `design-system` renders *"Waiting for a learned template…"* **above** *"Brand tokens, read out of your own email"* — the explanation arrives after the confusion, as filed. And `rfm-map` and `ab-readout` are two of the **only three** widgets carrying the full outcome+mechanism pair. `[instrumented]`

## Routed question answered — Echo → Nebula

> *"Should the missing-card four get retrofitted before fixtures, or after?"*

**Nebula: before — and it's seven, not four, and it isn't the same job as fixtures.** The retrofit serves the twenty widgets that will never be marketing assets but that every installed user meets on day one. Fixtures serve the three that ship. Different audiences, no shared dependency, no ordering conflict — retrofit first because it's cheaper and blocks nothing. It merges into Atlas's static-string item: same seven files, one edit, a card round the string he was already writing.

## Question for another pair — asked aloud

`~/code/pulsar/scripts/say.sh "Vector, Pulsar — here's what neither of us can settle. If the pane size arrives at runtime like Pulsar found, does the render-gate hero screenshot have a fixed height at all, or are we designing against a number the host hasn't told us yet? If it's the second, the hero must be composed at a size we choose, not captured at one we inherit — a different deliverable." --agent nebula`

**CHALLENGE (to Justin):** four of us have now said "not 23" and produced four different rosters. The disagreement isn't the count — nobody has named who the site visitor *is*. Pick that and the roster falls out. Leave it open and R3 re-runs this with a fifth roster.

— Nebula & Echo
