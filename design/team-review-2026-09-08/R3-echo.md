> Personas are fictional cognitive frames — see the pulsar-team SKILL.md §1 disclaimer. Nothing here is a statement by or about a real person.

# R3 — Echo · committed position

## The shared diagnosis

Nine reports, two cross-pollination rounds, and the number everyone landed on was never the argument. Nebula said five, then three. Vector said one. Iris said three, then four, then a conditional three-plus-one. I said two, then three. Four rosters at four counts, and Nebula named it straight: nobody said who the visitor is. Pick the person and the roster stops being a taste fight.

## THE VISITOR — named

**A Claude user who has never done lifecycle marketing and doesn't know what a render gate is.** Not the Braze/Klaviyo evaluator. Three reasons:

1. **The funnel says so.** `[judgement]` Orbit is free, account-gated, distributed through the Claude extension directory into yourorbit.team — not found by someone searching "Klaviyo alternative." The person who lands here clicked through from Claude's own gallery. A Claude user first; a lifecycle marketer, usually not yet.
2. **The site already bet this way.** `[judgement]` Iris's R1 finding 3: the homepage's "prove it works" section is `<OrbitDemo />`, an 870-line hand-scripted conversation teaching the mechanism from zero. Nobody built that for someone who already knows the category.
3. **My own instrument only has bite against this visitor.** The friend-repeatable test measures whether jargon survives a stranger. `send-calendar`'s "forward window"/"wall clock" only fails if the reader doesn't already speak ops — against a Braze admin it isn't a finding at all. The team built on that finding without challenging its premise; that was the round's tell.

Iris already drew the line in R2 without naming it: `cohort-retention` "belongs on /skills or a technical evaluator's tour, not the homepage sequence." That's visitor (a), parked correctly, elsewhere. This vote covers the homepage assets only.

## The roster, derived

**Hold at three: `render-gate` (FAIL fixture), `rfm-map`, `inbox-preview`.** One email, one list, through the gate — Nebula's recurrence argument, unchanged since R2.

Ran the test myself, live, at 900×520, on what's served today `[instrumented — Claude Browser, http://127.0.0.1:8899/]`:
- **`rfm-map`** — passes cold, empty, no fixture needed. Card: *"Where the money sits / Each segment placed by how recently and how often it buys."* Stranger's line: *shows which customers make you the most money.*
- **`render-gate`** — fails cold today: blank grey "MEASURING" box, "Waiting for HTML to gate…", no card. Confirms Nebula — sells nothing until its FAIL fixture exists.
- **`inbox-preview`** — fails cold too: names the tool (`orbit_score_subject_line`), not the outcome. Needs Nebula's planned fixture.

**The "false consensus" the story pair flagged is mostly resolved by the record — I'm closing the rest.** My R2 three and Iris's *R1* three genuinely disagreed by roster (only `rfm-map` shared) while both said "three." By end of R2, Iris had updated onto the identical unconditional trio, holding `esp-matrix` as a conditional fourth pending her fix-plus-changelog gate. Real convergence, not coincidence — one thread stays open: does `esp-matrix` clear? `[instrumented]` I opened `orbit-esp-matrix-html.data.html`: past the clip, the content reads "Bearer REST API key," "Liquid," "Api-Key request header (server-side key type)" — developer vocabulary, further from a non-expert Claude user than a lifecycle marketer even. Sentinel and Voyager's fix (delete the `100vh` pin) solves the clip; it doesn't touch this. **`esp-matrix` doesn't clear, clip or no clip.** It belongs with `cohort-retention` on the technical-evaluator page. Roster holds at three.

## Top concession

Vector's "name the number" lands harder than I credited in R1. My test is a design heuristic — it gates which candidates earn fixture time, nothing more. It is not a success metric, and Pulsar's R2 synthesis is a warning against exactly that mistake: a correct thing written down somewhere unexecutable. I concede Iris's CTA-click-through-by-`location` metric is the actual falsifier; my job ends where hers begins. Costs me the last word on whether this works — worth it, because an unmeasured heuristic is this review's recurring bug in a new widget.

## Line in the sand

Three, this roster, homepage only. Not `esp-matrix`, fixed or not. Not five jobs. A fourth needs a different visitor named for a different page, not a bigger inventory on this one.

## Vote — three principles

1. **Name the audience before the asset.** This round's entire cost was skipping it.
2. **Recurrence over inventory.** One story, many instruments, beats many stories told once — Nebula's rule, survived two rounds of challenge.
3. **A claim that can't fail a build is a note, not a decision.** Pulsar's throughline; applies to my own test as much as the changelog.

## Routed question, answered

Sentinel/Voyager asked the creative pair whether the render-gate hero composes at full height or the (fictional) 520px pane. Iris answered this in R2; reaffirming as load-bearing for my roster: **full height, no crop.** The line — "172px of your button is off-screen" — only lands if badge, hero box and BLOCKER line sit in one glance. A still isn't bound by a host pane that, per Sentinel/Voyager, doesn't even tell the truth about its own height.

## Open question for R4 — asked aloud

`~/code/pulsar/scripts/say.sh "Iris, now that Sentinel and Voyager say the clip fixes with one deleted CSS line — does esp-matrix go on the skills page for the technical evaluator once it's readable, or does someone still have to rewrite 'Bearer REST API key' into something a stranger's friend could repeat? Right now nobody owns that copy pass, and I don't want it to become the fourth thing this team writes down and never gates." --agent atlas`

— Echo
