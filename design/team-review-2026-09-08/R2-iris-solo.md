> Personas are fictional cognitive frames — see the pulsar-team SKILL.md §1 disclaimer. Nothing here is a statement by or about a real person.

# R2 — Iris solo · marketing cross-reference

## Go-to-market cross-reference

Six R1s converge on one shape: **the widget layer has surface area and zero proof it's ever been looked at.** Voyager and Vector `[instrumented]` each independently ran `grep -ic widget server/telemetry.js` → 0. Nothing tells us whether a human has opened any of these 23 panels since launch, in QA or in the wild — how Vector's disclosed esp-matrix defect survived eleven releases unflagged. Pulsar's F1/F2 `[instrumented]` reframes the clipping debate: the host hands every widget `hostContext.containerDimensions` at initialize, and Orbit never reads it (`grep -rn "getHostContext\|containerDimensions" server/ui/` → zero matches), so `100vh + overflow:hidden` turns *any* pane height into a hard clip. Echo `[instrumented]` found the actual sales mechanism — a bold outcome-subhead card above the "Waiting for…" line — already works on `rfm-map` and `ab-readout`, but is absent or misplaced on a third of the set. Atlas `[instrumented]` found three widgets rendering a genuine *void*, not an empty state, because their copy is JS-injected on data arrival with no static fallback. That's what marketing has to work with: a real signature move (esp-matrix's self-graded honesty legend, per Nebula), a real story mechanism (Echo's card), real blockers (Atlas's void-three, the disclosed esp-matrix defect), and zero usage evidence to arbitrate between candidates.

## ANSWER to Nebula's fork: one email, not five jobs

**One email through the whole gate.** Vector already narrowed this — "which ONE screenshot goes above the fold, and your render-gate FAIL is it" — and I'm extending his logic to the full asset set, not just the hero. `get-orbit/app/page.tsx:337-341` already runs `<OrbitDemo />`, an 870-line hand-scripted "five separate jobs" tour, and `:362` already runs `<IntegrationWall />`, a flat ESP logo grid covering the same six vendors as esp-matrix `[judgement — read app/page.tsx]`. A second disconnected-capability gallery is redundant with what's already shipped; it just swaps simulated screenshots for real ones. What the site has never shown a stranger is *one campaign surviving Orbit's actual pre-send gate* — subject line scored, render-gated, collision-checked, dark-mode-checked, in sequence, with a real near-miss caught along the way. That's the differentiated proof, and it's the only framing Nebula's own fixture design supports at the edges — her Merrowfield story already threads the same "Winter Restock" send through render-gate, inbox-preview, send-calendar and dark-pairs. She only forks to a second company (Calderpoint) for `cohort-retention`, which doesn't fit a single-email narrative by definition — that widget belongs on `/skills` or a technical evaluator's tour, not the homepage sequence.

## THE NUMBER — answering Vector by name

Vector: *"name the number that moves if this work lands... or scope to four items."* It's not none, and it's measurable today with no new instrumentation. `get-orbit/lib/db.ts:363` already has a `cta_clicks` table with a `location` column, populated from every `<OrbitCta location="...">` on the site (`components/orbit-cta.tsx`), joined to account creation and download rows further down the same file. **The number is CTA click-through rate for sessions that scroll to the new proof section, by `location` tag, against the existing hero and footer CTA baseline.** Ship the gate-sequence at a named location (e.g. `location="proof-strip"`), and the falsifiable claim is: click-through from that section beats the site's other mid-page CTA locations within one release cycle. If it doesn't, I say so and the section gets cut, not defended — the commitment Vector is asking for, not "make Orbit feel finished." I'm naming a leading indicator (the click), not download volume or activation, because Orbit is free and account-gated (`resolve-cta-state.ts`) — the bottleneck the site controls is the click, not what happens after.

## Findings with a marketing consequence others missed

**Vector's disclosed-defect finding is a brand-risk finding, not just a scope finding.** `get-orbit/lib/changelog.ts:236` (slug `0-32-0-integrations`, 31 Aug 2026), quoted: *"Three widgets are flagged and deliberately not fixed: the ESP capability matrix, the client simulator and the render gate all shrink their main content area to almost nothing in a short pane, worst on the ESP matrix, which drops to column headers and no data rows. The data is correct in all three; it is a legibility bug. Two attempted fixes made it worse and were reverted, so the defect is documented rather than guessed at a third time."* Vector filed this as scope pressure. I'm filing it as a gate: showing a stranger a clean esp-matrix screenshot while that page is still live and unamended is Orbit's own brand voice contradicting itself — this codebase's changelog corpus is built on "no more wrong-but-plausible" and "documented rather than guessed at a third time." **esp-matrix does not enter my four until the fix is verified live (not static-harness-only — Pulsar showed the harness number is unowned) and a matching changelog entry ships in the same release.** Nobody else connected the disclosure to the asset plan.

**Echo's card-mechanism finding retracts half of my own R1 shortlist.** I named `lifecycle-flow` as a hero-adjacent candidate for "fastest what does this even do answer." Echo's finding 1 lists it among the four widgets with **no outcome-subhead card at all** — title straight to grey box, failing her stranger test cold. I'm dropping it. `rfm-map` survives — Echo confirmed it passes with zero data on screen.

## Sharpened / retracted

**Retracted:** esp-matrix and lifecycle-flow as immediate candidates (above). **Sharpened:** my three becomes four, sequence-shaped not inventory-shaped — render-gate FAIL (hero, Nebula's fixture, Vector's pick), inbox-preview (score + truncation, Nebula's fixture), rfm-map (Echo-confirmed), esp-matrix held as a conditional fifth pending the fix-plus-changelog gate above.

**Voyager's 13-of-23 / Pulsar's "marketing not gated by the fix" — reconciled, and it sharpens rather than just confirms.** Pulsar is right for a reason nobody stated: for QA, 900×520 is an unverified guess at the live pane (Pulsar F1). For *marketing stills*, that objection doesn't apply — we render the fixture at whatever viewport we choose, since it's a curated screenshot, not a captured host session. Voyager's visibleFraction table is directly usable as a shot-list filter today, and we're freer than his numbers even suggest: a still can be composed at a taller crop no live pane will ever offer. This doesn't touch the esp-matrix gate above — that one's blocked by public disclosure, not by clipping.

## Question asked aloud, verbatim

`~/code/pulsar/scripts/say.sh "Nebula — one email through the whole gate, not five jobs. Your Merrowfield send is the spine. Build the fixtures for render-gate, inbox-preview and rfm-map first — esp-matrix waits on Sentinel and a changelog entry, not on your design time." --agent iris`

— Iris
