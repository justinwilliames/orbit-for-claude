> Personas are fictional cognitive frames — see the pulsar-team SKILL.md §1 disclaimer. Nothing here is a statement by or about a real person.

# R2 — Iris solo (Marketing cross-reference), 31 Aug 2026

## Verdict

The brand and channel story is fine — Echo's right that the one-sentence retell survives — but there is no funnel to cross-reference against, because there is no funnel. Zero stars, zero forks, one issue that's an SEO bot, telemetry with no install-source field, and a GitHub release counter that can't tell a registry install from a manual download from someone re-downloading the same version twice. Every number I'd need to answer this round with data instead of judgement is either missing or, worse, un-buildable from what's in this repo. That's not a brand problem. It's a measurement floor the review keeps building stories on top of.

## 1. Where the story holds and where it breaks

**Holds:** Echo's one-sentence test — "a lifecycle marketer built into Claude that finds real render bugs and turns your own emails into a design system, free, no key" — survives a retell because it has a proof point that isn't a claim. I agree with his read and I'll add the marketing reason it works: it names the *mechanism* (turns your emails into the system) not the *category* (lifecycle tool), and mechanism-claims are the ones a stranger can repeat without sounding like they're reciting ad copy.

**Breaks:** the front door the story runs through is wrong. Nebula, Voyager, Sentinel and the orchestrator all independently confirm the README undercounts the library (91 vs 99 guides), the GitHub description undercounts twice over (80/130 live vs 83/135 shipped vs 81/135 this MCP session advertises — three numbers, simultaneously true, for "how big is this"), and the one install instruction on the page names a fully deprecated registry entry. A story this good sitting behind a front door this broken is the finding: nobody has stress-tested the path from "stranger reads this" to "stranger has it running," because functionally no stranger has ever walked it. Sentinel's reproducibility gap (the shipped guides-export.json doesn't match its own tag) means even the number I'd want to fix isn't stable enough to promise.

## 2. R1 findings with a marketing consequence the other lenses missed

**Vector's 106 installs across v0.29.2–v0.31.1 during an 11-day silent blackout** is a marketing fact, not just a process one: people found this and installed it *without* anyone telling them to. That's organic pull against a broken front door and no live promotion — the actual demand signal this review has been missing. Nobody framed it that way; Vector filed it as evidence the ship-nothing guard cost real installs, which is true, but it's also the only proof I have that word-of-mouth or search discovery works at all for this product. I'm not going to build a channel plan on eleven days of data — that's exactly my scar — but it's worth someone (Voyager, per his own CONSULT to Vector) running the client-ID-activation query, because right now that number is the entire evidence base for "does anyone want this."

**Nova's icon and Sentinel's stale-proof-shot findings** are brand-trust findings with a specific funnel cost: a dark-mode-identical icon and 19-day-stale render-gate screenshots are the two visual assets most likely to appear *outside* this repo — in a marketplace listing, a tweet, a "cool MCP servers" roundup. If Decision #4 (tell someone) ever gets actioned, those are the assets that travel first, and both are currently wrong in ways a stranger would notice before reading a word of copy.

## 3. Routed questions

**To Nebula, by name — Q5.** You're right that README:8's employer-credit opener is copyable and README:21's self-indicting gate story isn't. I don't have funnel data to prove which converts — traffic is functionally zero, so there's no A/B signal, and I'm not going to invent one. Judgement call, stated as judgement: employer credits do recognition work for a stranger who's never heard of this and needs a reason to keep reading past line one; the gate story does credibility work for a stranger who's already reading and deciding whether to trust the rigor claim. Cold traffic — which is all we have — needs the recognition beat first. But README:8 doesn't have to be *either/or* with README:21; right now the gate story is buried at line 21 doing no work for anyone who bounces before it. I'd keep the credit line as the opener and move the gate story up, not cut it — it's the only proof point in the whole page nobody else can copy, and it's currently positioned like a footnote.

**To Echo, by name — Q6.** No. Confirmed, not inferred: Voyager's F2 shows `trackToolCall`'s wire payload carries `type, slug, version, clientId` — no `ok`, no `verdict` — and I read PRIVACY.md myself, four event types, same four fields, nothing else. If we point a stranger at this today, we can count that they started a session and that a tool errored. We cannot tell whether their render gate passed, whether their setup completed, or whether they got value in the first ten minutes. Pointing someone here right now is exactly the blind ok-path the 12 Aug review flagged — nothing has changed on that axis since. This is Voyager's open CONSULT to Meridian (the `verdict` field, Decision #2, eighteen days unanswered) and it has to resolve before Decision #4 does, or "tell someone" produces installs we can't read.

**To Pulsar, by name — Q9. Highest-stakes question in the round, answered on the evidence that exists and flagged where it doesn't.** I checked whether the registry name and the GitHub release are the same or separate distribution channels: every version's registry entry — deprecated ones included — carries a `packages[].identifier` that resolves to the *same* GitHub release asset URL (`github.com/.../releases/download/vX.Y.Z/orbit-lifecycle-marketing-system-for-claude.mcpb`). There is no separate artifact behind the dead name. The release-download counts I pulled (`gh release view`, 42 on v0.31.1, 3 on v0.32.0 so far) are aggregate across every install path — registry, manual, extension directory — and cannot be decomposed by which name a client resolved through, because nothing in `telemetry.js` or `PRIVACY.md` carries an install-source field. **So I cannot prove traffic is or isn't flowing through `braze-lifecycle-mcp` — that evidence does not exist in this repo, and I won't invent it.**

What I can say from mechanism, not guesswork: the deprecated registry entry's own `_meta` already carries a machine-readable redirect — `"statusMessage":"Renamed to io.github.justinwilliames/orbit-lifecycle-mcp. Install io.github.justinwilliames/orbit-lifecycle-mcp instead."` — for every one of its eight versions. That redirect exists at the protocol layer regardless of what the README says. Fixing the README's string doesn't remove a channel, because the README's string was never the channel — the shared release artifact is, and it's reachable from three places (registry resolution, manual download, extension directory) none of which depend on the README continuing to name the wrong entry. Pulsar's fear — "correcting the README cuts the only channel that's ever produced an install" — has no mechanism that would make it true. **Fix it.** What I can't verify from here is whether Claude Desktop's own install UI surfaces that `statusMessage` to a human before install (client behavior, outside this repo) — `unverified — routed to Voyager or whoever owns the client-facing integration test`.

## 4. The falsifiable number

Checked the surface exists before naming it: `gh release view v0.32.0 --json assets` returns a real, live, unauthenticated `downloadCount` field, currently **3** (day one). v0.31.1 sits at **42**, reached over an 11-day window from a cold, silent, un-promoted release.

**The number: v0.32.0's cumulative release-asset download count clears 42 within 11 days of today (by 2026-09-11), the same window and the same silence v0.31.1 needed to get there.** If the README/description fixes land this cycle and that number *doesn't* clear 42 in the same window with no active promotion, the front-door fixes bought us nothing measurable and Decision #4 (tell someone) stops being optional — it becomes the only lever left.

## 5. Consult into R3

**CONSULT Meridian:** you're already in R3 answering Voyager's `verdict`-field consent question. Fold in one more, same shape: does adding a closed-vocabulary `installSource` field (`registry` | `manual` | `extension-directory`) — no arguments, no content — sit inside the existing telemetry disclosure, or does it need its own re-consent? It's the only way Echo's and Pulsar's questions ever get answered with a number instead of a mechanism argument.

— Iris
