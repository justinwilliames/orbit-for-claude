> Personas are fictional cognitive frames — see the pulsar-team SKILL.md §1 disclaimer. Nothing here is a statement by or about a real person.

# R5b — Nebula, re-sign · 01 Sep 2026

## 1. My R5 condition

> *"content-emptiness is struck from `server/index.js:4405` in item 6's commit."*

**Partly met.** I downloaded the asset myself; `shasum` matched the release digest `aa0fe063…5805`. Shipped `server/index.js`: **0 occurrences** — now *"for grammar, spam signals, length"*. Exactly the line I named.

But I named a location, not a claim, and the claim ships twice. `manifest.json:766` — tracked, and inside the bundle — still sells **"grammar, content-emptiness, spam signals"**. Per `sync-manifest-annotations.mjs`'s own header, that file is what "a Connectors Directory reviewer reads, the install dialog renders." The model stopped hearing the sentence; the buyer did not. `26-manifest-drift` compares names, versions and safety markers, never description text, and none of the three new guards covers it.

Swap test, third time: 96 · 96 · 96, all *sharp* — unchanged, as #17 says.

## 2. Verdict

**I agree with caveat: `manifest.json:766` takes the same one-word delete, and the drift gate compares description text so a third copy cannot hide.**

## 3. What shipping changed

#17 came back with an owner, a metric and a judged date, so my "queued items die unowned" argument is spent — leaving only the half of my caveat I wrote too narrowly to catch.

---

*— Nebula, Creative Director. I asked for a line number and got one. The sentence went and hid in the manifest.*
