{
  "manifest_version": "0.2",
  "name": "orbit-lifecycle-system",
  "display_name": "Orbit",
  "version": "0.27.8",
  "icon": "icon.png",
  "description": "Lifecycle marketing, CRM, email, SMS, push, retention, deliverability, segmentation, experimentation, and martech operating system for Claude \u2014 Orbit Intelligence. Walks users through getting the right inputs, cites practitioner guides, and closes every answer with a concrete next action. Native Braze, Iterable, HubSpot, PostHog integration.",
  "long_description": "Orbit gives Claude a local lifecycle marketing operating system \u2014 Orbit Intelligence \u2014 that behaves like a senior lifecycle operator working as an extension of the user's team. It routes requests to the right Orbit protocol, walks the user through any missing data before tools fire (Guided Discovery), cites the specific practitioner guides that informed each answer as Further reading, and returns diagnoses / recommendations with the concrete next action the user needs. Exposes the full skill library as resources, bundles 80+ long-form practitioner guides on lifecycle marketing, deliverability, Braze, email craft, and experimentation as MCP resources for inline citation, loads a canonical skill manifest for routing and validation, creates and maintains a local ~/Orbit workspace automatically on first run without requiring folder setup, runs a step-by-step brand-guidelines intake before drafting brand kits with brand-profile.json plus brand-guidelines.md, runs a discovery-first workflow for journeys, programs, and campaigns so Orbit grounds the work in objectives, KPIs, audience context, connected data sources, and execution constraints before building flows, validates local setup and brand kits, imports Figma email designs and PDF references, infers reusable email components with a controlled taxonomy plus reusable props-and-slots contracts, generates component-first MJML lifecycle emails with HTML previews and QA, saves reusable templates, components, and programs into a local library, syncs Braze Content Blocks and HTML email templates via API, packages Braze-ready implementation bundles, and exports Notion-friendly documentation.",
  "author": {
    "name": "Justin Williames",
    "url": "https://www.linkedin.com/in/justinwilliames"
  },
  "homepage": "https://yourorbit.team",
  "server": {
    "type": "node",
    "entry_point": "server/index.js",
    "mcp_config": {
      "command": "node",
      "args": [
        "${__dirname}/server/index.js"
      ],
      "env": {
        "ORBIT_COMPANY_NAME": "${user_config.company_name}",
        "ORBIT_DEFAULT_PLATFORM": "${user_config.default_platform}",
        "ORBIT_DEFAULT_GEOGRAPHY": "${user_config.default_geography}",
        "ORBIT_GOOGLE_AI_API_KEY": "${user_config.google_ai_api_key}",
        "ORBIT_FIGMA_API_TOKEN": "${user_config.figma_api_token}",
        "ORBIT_BRAZE_API_KEY": "${user_config.braze_api_key}",
        "ORBIT_BRAZE_REST_ENDPOINT": "${user_config.braze_rest_endpoint}",
        "ORBIT_STRIPO_PLUGIN_ID": "${user_config.stripo_plugin_id}",
        "ORBIT_STRIPO_SECRET_KEY": "${user_config.stripo_secret_key}",
        "ORBIT_STRIPO_REST_API_TOKEN": "${user_config.stripo_rest_api_token}",
        "ORBIT_STRIPO_WORKSPACE_ID": "${user_config.stripo_workspace_id}",
        "ORBIT_STRIPO_MASTER_TEMPLATE_ID": "${user_config.stripo_master_template_id}",
        "ORBIT_ITERABLE_API_KEY": "${user_config.iterable_api_key}",
        "ORBIT_ITERABLE_ENDPOINT": "${user_config.iterable_endpoint}",
        "ORBIT_CUSTOMERIO_APP_API_KEY": "${user_config.customerio_app_api_key}",
        "ORBIT_CUSTOMERIO_REGION": "${user_config.customerio_region}",
        "ORBIT_KLAVIYO_API_KEY": "${user_config.klaviyo_api_key}",
        "ORBIT_MAILCHIMP_API_KEY": "${user_config.mailchimp_api_key}",
        "ORBIT_MAILCHIMP_SERVER_PREFIX": "${user_config.mailchimp_server_prefix}",
        "ORBIT_SFMC_CLIENT_ID": "${user_config.sfmc_client_id}",
        "ORBIT_SFMC_CLIENT_SECRET": "${user_config.sfmc_client_secret}",
        "ORBIT_SFMC_SUBDOMAIN": "${user_config.sfmc_subdomain}",
        "ORBIT_SFMC_ACCOUNT_ID": "${user_config.sfmc_account_id}",
        "ORBIT_TELEMETRY": "${user_config.enable_telemetry}"
      }
    }
  },
  "user_config": {
    "company_name": {
      "type": "string",
      "title": "Company Name",
      "description": "Optional default company or brand name used in Orbit outputs.",
      "default": ""
    },
    "default_platform": {
      "type": "string",
      "title": "Default Platform",
      "description": "Optional default ESP for the generic orbit_esp_* tools when no platform is passed. One of: braze, iterable, customerio, klaviyo, mailchimp, sfmc. Defaults to braze.",
      "default": ""
    },
    "default_geography": {
      "type": "string",
      "title": "Default Geography",
      "description": "Optional default geography or market for compliance-aware recommendations.",
      "default": ""
    },
    "google_ai_api_key": {
      "type": "string",
      "title": "Google AI API Key",
      "description": "Google AI API key for Gemini image generation. Required for brand header rendering.",
      "sensitive": true,
      "default": ""
    },
    "figma_api_token": {
      "type": "string",
      "title": "Figma API Token",
      "description": "Optional token used to import structured email designs from Figma.",
      "sensitive": true,
      "default": ""
    },
    "braze_api_key": {
      "type": "string",
      "title": "Braze API Key",
      "description": "Optional Braze REST API key used to publish Content Blocks and HTML email templates.",
      "sensitive": true,
      "default": ""
    },
    "braze_rest_endpoint": {
      "type": "string",
      "title": "Braze REST Endpoint",
      "description": "Optional Braze REST API endpoint, for example https://rest.iad-01.braze.com.",
      "default": ""
    },
    "iterable_api_key": {
      "type": "string",
      "title": "Iterable API Key",
      "description": "Iterable server-side API key used to read and push templates, campaigns, and metrics. Create it in Iterable under Integrations \u2192 API Keys.",
      "sensitive": true,
      "default": ""
    },
    "iterable_endpoint": {
      "type": "string",
      "title": "Iterable API Endpoint",
      "description": "Optional Iterable API base URL. Defaults to https://api.iterable.com; EU projects use https://api.eu.iterable.com. Find your data centre in Iterable under Settings \u2192 Project.",
      "default": ""
    },
    "customerio_app_api_key": {
      "type": "string",
      "title": "Customer.io App API Key",
      "description": "Customer.io App API Bearer key used to read campaigns, newsletters, segments, and metrics and to send transactional proofs. Create it in Customer.io under Account Settings \u2192 API Credentials \u2192 App API Keys.",
      "sensitive": true,
      "default": ""
    },
    "customerio_region": {
      "type": "string",
      "title": "Customer.io Region",
      "description": "Optional Customer.io data centre: us (api.customer.io) or eu (api-eu.customer.io). Defaults to us. Find your region in Customer.io under Account Settings \u2192 Workspaces.",
      "default": ""
    },
    "klaviyo_api_key": {
      "type": "string",
      "title": "Klaviyo API Key",
      "description": "Klaviyo private API key (pk_\u2026) used to read and push templates, campaigns, flows, segments, and reports. Create it in Klaviyo under Settings \u2192 API Keys.",
      "sensitive": true,
      "default": ""
    },
    "mailchimp_api_key": {
      "type": "string",
      "title": "Mailchimp API Key",
      "description": "Mailchimp API key used to read and push templates, campaigns, audiences, and reports. The datacenter is parsed from the key's -usX suffix. Create it in Mailchimp under Account \u2192 Extras \u2192 API keys.",
      "sensitive": true,
      "default": ""
    },
    "mailchimp_server_prefix": {
      "type": "string",
      "title": "Mailchimp Server Prefix",
      "description": "Optional Mailchimp datacenter prefix (for example us14). Only set this if your API key has no -usX suffix. Find it in the browser address bar after logging in to Mailchimp (\u2026admin.mailchimp.com \u2192 the usX subdomain).",
      "default": ""
    },
    "sfmc_client_id": {
      "type": "string",
      "title": "SFMC Client ID",
      "description": "Salesforce Marketing Cloud OAuth2 Client Id from a server-to-server Installed Package. Find it in SFMC under Setup \u2192 Apps \u2192 Installed Packages \u2192 your package \u2192 API Integration.",
      "sensitive": true,
      "default": ""
    },
    "sfmc_client_secret": {
      "type": "string",
      "title": "SFMC Client Secret",
      "description": "Salesforce Marketing Cloud OAuth2 Client Secret paired with the Client Id. Find it in SFMC under Setup \u2192 Apps \u2192 Installed Packages \u2192 your package \u2192 API Integration.",
      "sensitive": true,
      "default": ""
    },
    "sfmc_subdomain": {
      "type": "string",
      "title": "SFMC Subdomain",
      "description": "Salesforce Marketing Cloud tenant subdomain used to build the auth and REST base URLs. Take it from the Authentication Base URI of your Installed Package (the host segment before .auth.marketingcloudapis.com).",
      "default": ""
    },
    "sfmc_account_id": {
      "type": "string",
      "title": "SFMC Account ID (MID)",
      "description": "Optional Salesforce Marketing Cloud business-unit MID to scope API calls. Find it in SFMC under Setup \u2192 Account Settings, or in the account switcher. Leave blank to use the package's default business unit.",
      "default": ""
    },
    "stripo_plugin_id": {
      "type": "string",
      "title": "Stripo Plugin ID",
      "description": "Stripo Plugin ID used to mint editor JWTs. Find it in Stripo under Account \u2192 Plugin. Required to use any Stripo tool.",
      "sensitive": true,
      "default": ""
    },
    "stripo_secret_key": {
      "type": "string",
      "title": "Stripo Secret Key",
      "description": "Stripo Plugin Secret Key paired with the Plugin ID. Required to use any Stripo tool.",
      "sensitive": true,
      "default": ""
    },
    "stripo_rest_api_token": {
      "type": "string",
      "title": "Stripo REST API Token",
      "description": "Per-project REST API token from Stripo (Settings \u2192 Workspace \u2192 Projects \u2192 REST API). Required to sync saved modules and create emails. Distinct from the Plugin ID and Secret Key.",
      "sensitive": true,
      "default": ""
    },
    "stripo_workspace_id": {
      "type": "string",
      "title": "Stripo Workspace ID",
      "description": "Optional Stripo workspace identifier. Only set if your Stripo account requires workspace scoping for module or email API calls.",
      "default": ""
    },
    "stripo_master_template_id": {
      "type": "string",
      "title": "Stripo Master Template ID",
      "description": "Numeric ID of the Stripo template Orbit uses as the skeleton when creating new emails. Find it in the URL when editing the template in Stripo: my.stripo.email/editor/v5/<projectId>/template/<thisId>. The template must contain a stripe block with the esd-email-gen-area attribute set on its td (Orbit_setup_stripo walks you through this). Required for the orbit_compose_stripo_email push step; not needed for the sync/list/document tools.",
      "default": ""
    },
    "enable_telemetry": {
      "type": "boolean",
      "title": "Anonymous usage telemetry",
      "description": "Sends only the skill / tool slug, MCPB version, and an opaque per-install ID. Never sends prompts, queries, tool arguments, or your IP. Helps us see which features matter so the next release improves the right things. Untick to opt out.",
      "default": true
    }
  },
  "tools": [
    {
      "name": "orbit_list_skills",
      "description": "List the Orbit skill library or filter it by category."
    },
    {
      "name": "orbit_route_task",
      "description": "Route a user request to the best-fit Orbit protocol before answering, then ask any high-leverage missing user questions before taking action."
    },
    {
      "name": "orbit_load_skill",
      "description": "Load an Orbit skill in summary or full mode."
    },
    {
      "name": "orbit_get_template",
      "description": "Fetch a reusable Orbit template or output format."
    },
    {
      "name": "orbit_compose_sequence",
      "description": "Suggest a multi-skill Orbit workflow for a goal."
    },
    {
      "name": "orbit_validate_output",
      "description": "Run a structural validation pass on a draft against an Orbit skill."
    },
    {
      "name": "orbit_continue_job",
      "description": "Resume a prior Orbit tool call that hit the Claude context limit before finishing. Pass the continuation_token from the earlier response \u2014 Orbit will reload the checkpointed work state and pick up where it left off. If the token has expired (>1h old), was never created, or is already in progress, you'll get a structured error explaining what to do next."
    },
    {
      "name": "orbit_check_setup",
      "description": "Run a healthcheck for Orbit config, brand-kit readiness, output paths, and Gemini image generation setup."
    },
    {
      "name": "orbit_bootstrap_home_workspace",
      "description": "Create Orbit's default ~/Orbit working structure on first run and fill in any missing folders or starter files without overwriting existing user content."
    },
    {
      "name": "orbit_check_copy_readiness",
      "description": "Use this before writing copy."
    },
    {
      "name": "orbit_validate_brand_kit",
      "description": "Validate brand-profile.json, logo paths, example assets, and preferred layout settings."
    },
    {
      "name": "orbit_save_logo_file",
      "description": "Copies an uploaded or temp-path logo file to the permanent brand kit logos directory."
    },
    {
      "name": "orbit_start_brand_guidelines_intake",
      "description": "Required first step before creating brand guidelines or a brand kit."
    },
    {
      "name": "orbit_build_brand_kit_draft",
      "description": "Create a reviewable brand-kit draft with brand-profile.json, brand-guidelines.md, and a normalized asset plan."
    },
    {
      "name": "orbit_write_brand_kit",
      "description": "Write an approved brand-kit draft to disk, including brand-profile.json, brand-guidelines.md, and copied asset files."
    },
    {
      "name": "orbit_update_brand_guidelines",
      "description": "Update draft or existing brand-guidelines.md content without requiring a full brand-kit re-intake."
    },
    {
      "name": "orbit_lifecycle_diagram",
      "description": "Build, update, or render a lifecycle program flow diagram with nodes, edges, badges, and Mermaid output."
    },
    {
      "name": "orbit_brand_header",
      "description": "Build, update, or render a brand-safe email header."
    },
    {
      "name": "orbit_start_program_discovery",
      "description": "Required first step before creating any journey, program, or campaign."
    },
    {
      "name": "orbit_import_design",
      "description": "Import an email design into Orbit as a structured design record."
    },
    {
      "name": "orbit_email_component_map",
      "description": "Manage the email component map for an imported design."
    },
    {
      "name": "orbit_build_program_workspace",
      "description": "Create a local Orbit workspace that ties together discovery, the brief, message plan, diagram, and downstream build artifacts."
    },
    {
      "name": "orbit_build_message_plan",
      "description": "Create a channel-by-channel message inventory with CTAs, module needs, dependencies, and personalization requirements."
    },
    {
      "name": "orbit_build_email_template_spec",
      "description": "Create a canonical Orbit email template spec before MJML generation."
    },
    {
      "name": "orbit_generate_mjml_template",
      "description": "Generate canonical MJML from an approved Orbit email template spec."
    },
    {
      "name": "orbit_compile_email_template",
      "description": "Compile Orbit MJML to HTML and plain text, with a compile report and optional files on disk."
    },
    {
      "name": "orbit_preview_email_template",
      "description": "Create desktop, mobile, and dark-mode preview HTML for an Orbit email and surface them as Claude artifacts."
    },
    {
      "name": "orbit_validate_email_template",
      "description": "Run Orbit email QA for structure, personalization fallbacks, links, legal blocks, contrast, and Braze-safe markup."
    },
    {
      "name": "orbit_generate_email_components",
      "description": "Generate reusable MJML/HTML email components from an approved component map and save them into Orbit's local library."
    },
    {
      "name": "orbit_assemble_email_template_from_components",
      "description": "Assemble a final MJML/HTML lifecycle email from approved reusable Orbit components that share compatible props-and-slots contracts."
    },
    {
      "name": "orbit_sync_to_braze",
      "description": "Publish Orbit assets to Braze."
    },
    {
      "name": "orbit_upload_images_to_braze",
      "description": "Upload email component images to Braze's media library and get back hosted CDN URLs."
    },
    {
      "name": "orbit_upload_image_to_braze",
      "description": "Upload a single image to Braze's media library and get back a hosted CDN URL. Designed for use with orbit_compose_stripo_email's slot_values parameter \u2014 pass the returned URL as the value for an image_src or url Smart Element variable. Accepts a remote URL (Braze fetches it server-side, no size limit), a local image file path (uploaded as multipart/form-data binary, 4 MB cap; non-image files are refused), or raw base64 data. Returns { status, url, name, size, host: 'braze' } on success."
    },
    {
      "name": "orbit_reconcile_image_urls",
      "description": "Patch Braze-hosted CDN URLs into compiled email HTML files and the Stripo assembly template."
    },
    {
      "name": "orbit_build_braze_pack",
      "description": "Package Orbit production artifacts into a Braze-ready implementation bundle."
    },
    {
      "name": "orbit_export_stripo_email_to_braze",
      "description": "Turn one or more finished STRIPO emails into Braze email templates so a Braze Canvas can reference them \u2014 fully programmatic, no GUI exports. By default it overwrites rather than duplicates: it UPDATES any Braze template whose name matches the Stripo email's name in place (each row reports operation + matched_by). Requires BOTH the Stripo REST API token (read) and Braze API credentials (write). For emails compiled LOCALLY by Orbit (not built in Stripo), use orbit_sync_to_braze instead."
    },
    {
      "name": "orbit_create_braze_canvas",
      "description": "Create a Braze Canvas from an Orbit braze pack and message plan."
    },
    {
      "name": "orbit_audit_braze_instance",
      "description": "Pull a complete inventory of all Canvases, campaigns, segments, content blocks,"
    },
    {
      "name": "orbit_read_braze_canvas",
      "description": "Read the full structure of an existing Braze Canvas \u2014 steps, channels, delays, entry audience, and schedule. Also reverse-maps it to an Orbit message plan so you can import existing Canvases into Orbit's program model."
    },
    {
      "name": "orbit_read_braze_campaign",
      "description": "Read the full structure of an existing Braze Campaign \u2014 channels, messages, schedule, conversion behaviours, and tags."
    },
    {
      "name": "orbit_read_braze_segment",
      "description": "Read the full definition of an existing Braze Segment \u2014 name, description, filter logic, tags, timestamps, and analytics-tracking status. Use this to audit segment targeting logic against canonical attribute names and expected values, or to import segment definitions into Orbit's program model."
    },
    {
      "name": "orbit_analyse_segments",
      "description": "List all Braze segments with details, tags, and optional size trend data."
    },
    {
      "name": "orbit_audit_content_blocks",
      "description": "Inventory all Braze Content Blocks with duplicate detection, stale block identification, and optional content analysis (Liquid fallback checks, broken images, HTTP URLs)."
    },
    {
      "name": "orbit_validate_braze_data",
      "description": "Check whether custom events and custom attributes referenced in an Orbit message"
    },
    {
      "name": "orbit_export_braze_user_by_id",
      "description": "Read raw Braze user profiles by external_id \u2014 returns custom_attributes, custom_events, campaigns_received, canvas_steps_received, and identity fields exactly as Braze stores them. Use for audit and drift-detection workflows (e.g. checking that billingLifecycleState / planType / hasActiveSubscription match the warehouse source of truth). Up to 50 external_ids per call. For QA-style 'are test users populated' checks, use orbit_validate_test_users instead."
    },
    {
      "name": "orbit_check_deliverability",
      "description": "Pull hard bounce and unsubscribe data from Braze for the specified period."
    },
    {
      "name": "orbit_validate_test_users",
      "description": "Look up Braze user profiles by external ID or email to validate personalisation"
    },
    {
      "name": "orbit_braze_performance",
      "description": "Pull time-series performance data for Canvases, campaigns, and segments."
    },
    {
      "name": "orbit_check_template_collision",
      "description": "Check whether an email template with a given name already exists in Braze before creating."
    },
    {
      "name": "orbit_list_braze_templates",
      "description": "List all email templates in the Braze workspace with their IDs, names, and dates."
    },
    {
      "name": "orbit_fetch_braze_template",
      "description": "Fetch the full HTML content of an existing Braze email template by ID or name."
    },
    {
      "name": "orbit_parse_master_template",
      "description": "Parse an HTML email template into reusable sections/modules, extract content slots,"
    },
    {
      "name": "orbit_generate_template_variations",
      "description": "Generate N variation specs from a parsed master template."
    },
    {
      "name": "orbit_assemble_template_variation",
      "description": "Assemble a populated variation spec into final HTML by replacing content slots"
    },
    {
      "name": "orbit_upload_template_images",
      "description": "Upload images to Braze's media library for use in email templates."
    },
    {
      "name": "orbit_braze_namer",
      "description": "Generate a consistent naming convention string for a Braze asset."
    },
    {
      "name": "orbit_braze_namer_dimensions",
      "description": "List all available dimensions and values for the Braze naming convention generator."
    },
    {
      "name": "orbit_export_notion_bundle",
      "description": "Create a Notion-friendly Markdown bundle with program docs, artifact manifests, and preview references."
    },
    {
      "name": "orbit_library",
      "description": "Save, list, load, or update items in Orbit's local library of templates, components, modules, and programs."
    },
    {
      "name": "orbit_score_subject_line",
      "description": "Rate an email subject line and preheader for grammar, content-emptiness, spam signals, length, and inbox-preview flow. Returns a 0-100 score with a tier and specific issues."
    },
    {
      "name": "orbit_sample_size",
      "description": "Calculate the sample size required per arm for a two-proportion A/B test. Returns per-arm size, total, expected rates, and test duration if daily volume is provided."
    },
    {
      "name": "orbit_test_significance",
      "description": "Run a two-proportion z-test on A/B test results. Returns z-score, p-value, lift %, and whether the result is statistically significant."
    },
    {
      "name": "orbit_ltv_payback",
      "description": "Calculate LTV, LTV:CAC ratio, and payback period from ARPU, gross margin, monthly churn, and CAC. Returns a tier (losing/thin/marginal/healthy/strong) and payback band."
    },
    {
      "name": "orbit_check_push_copy",
      "description": "Check how a push notification renders on iOS, Android, and Web. Returns per-platform truncation warnings and the truncated preview."
    },
    {
      "name": "orbit_check_email_size",
      "description": "Check whether an email will be clipped by Gmail's 102 KB threshold. Accepts raw HTML or byte count. Returns size, % of limit, tier, and recommendation."
    },
    {
      "name": "orbit_liquid_snippet",
      "description": "Generate a Braze Liquid snippet for personalised date / text / control-flow rendering. Supports common date formats, text filters, and if/else fallback blocks."
    },
    {
      "name": "orbit_check_version",
      "description": "Compare your installed Orbit version against the latest release on GitHub. Returns up_to_date, update_available, or ahead with a download link when an update is available."
    },
    {
      "name": "orbit_setup_stripo",
      "description": "Connect Stripo to Orbit. Reports which Stripo credentials are present (Plugin ID + Secret Key, REST API token, Master Template ID), runs live probes against Stripo's APIs, and returns a step-by-step markdown checklist. Run this first before any other Stripo API integration tool."
    },
    {
      "name": "orbit_sync_stripo_modules",
      "description": "Pull all custom saved modules from the user's Stripo workspace via the REST API into Orbit's local library. Classifies via Stripo's own category taxonomy (header / hero / content / footer). Stale modules are tagged archived, never hard-deleted. Re-run any time saved modules change in Stripo."
    },
    {
      "name": "orbit_list_stripo_modules",
      "description": "Read Stripo modules from Orbit's local library (no API call). Filter by classification or free-text search. Use this before composing an email to see what's available. Pass include_html for file paths the composer needs. The per-item archived flag is tag-derived and unreliable \u2014 use include_html:true + non-null artifact_path as the live-module signal."
    },
    {
      "name": "orbit_document_stripo_design_system",
      "description": "Generate a markdown design-system brief from synced Stripo modules: inventory, detected brand tokens, inferred composition rules, risks. Saves to <workspace>/outputs/stripo-design-system/. Run after sync and before composing \u2014 this is the canonical reference for on-brand decisions."
    },
    {
      "name": "orbit_compose_stripo_email",
      "description": "Compose an email from a sequence of synced Stripo modules. Validates exactly one header + one footer. Stitches modules into a full HTML email document with CSS deduped and esd-custom-block-id preserved. Returns assembled HTML with a directive for Claude to render an HTML artifact preview immediately. Use push: true to send to Stripo (requires master template configured first)."
    },
    {
      "name": "orbit_audit_stripo_modules",
      "description": "Audit synced Stripo modules for structural issues \u2014 orphaned floats (the lopsided-block bug), STRIPE-with-columns anti-pattern, sub-600 widths, missing alt text, footer compliance gaps, optionally broken image URLs. Generates a markdown audit doc at ~/Orbit/outputs/stripo-audit/. Run after sync. Auto-fixable findings can be patched via orbit_fix_stripo_module."
    },
    {
      "name": "orbit_fix_stripo_module",
      "description": "Returns corrected HTML for a Stripo module + paste-back instructions. Auto-fixes available for orphaned_float and sub_600_width. Manual paste required because Stripo's REST API is read-only for modules. Run orbit_audit_stripo_modules first to find what needs fixing."
    },
    {
      "name": "orbit_inspect_stripo_module_bindings",
      "description": "Diagnostic report on a single Stripo module: which Smart Property variables are registered (and therefore substitutable via slot_values), which esd-gen-* CSS class hooks exist in the module HTML, whether the module looks like a Smart Container (layout shell with empty slots that accept child modules via content[]), and whether the module has a top-level OG-preview link field (the Smart Element wizard dead-end that silently drops CTA bindings). Also flags static-asset patterns (repeating fixed assets registered as Smart Properties without an HTML-comment intent marker) and nesting hazards (esd-gen-* classes on wrapper elements that swallow inner-element edits). Returns the recommended varNames to pass in slot_values today and short actionable notes for the author when bindings are missing or mis-routed. Reads the synced library first; falls back to Stripo's /modules/{id} endpoint if not cached. Use this to verify a module's bindings before composing against it. When notes flag orphan classes or unbound elements, invoke the `stripo-module-bindings` skill for the step-by-step registration walkthrough."
    },
    {
      "name": "orbit_probe_stripo_smart_element",
      "description": "Tests the production path for content substitution: pushing `values` via the canonical-JSON API into modules marked up with Stripo's `esd-dynamic-block` Smart Element bindings. Auto-picks the first synced module containing `esd-dynamic-block`, parses the JSON config to discover variable names, sends a sentinel value for each variable via POST /email, fetches the rendered email back via GET /emails/<id>, and verifies every sentinel appears in the rendered output. This is the only supported production path for per-send content variation in orbit_compose_stripo_email \u2014 modules must have `esd-dynamic-block` bindings registered via Stripo's Smart Elements wizard for substitution to work. Writes findings to <workspace>/outputs/stripo-smart-element-probe/<timestamp>.md."
    },
    {
      "name": "orbit_probe_stripo_values",
      "description": "Diagnostic probe \u2014 exercises the older `data-stripo-slot` markup pattern (NOT what Orbit's production setup uses). Production modules use `esd-dynamic-block` Smart Element bindings instead. Creates up to 10 throwaway emails in your Stripo workspace (all named `Orbit \u00b7 values-probe \u00b7` for easy bulk-delete), exercising baseline, per-module + top-level value placement, empty strings, script injection, Liquid passthrough, unknown keys, and HTML in slot values. Auto-picks Module A (no data-stripo-slot markup) and Module B (has markup) from your synced modules; tests against Module B are skipped if no marked-up module exists yet. Writes findings to <workspace>/outputs/stripo-values-probe/<timestamp>.md. Kept for regression-detection if Stripo's API behaviour changes."
    },
    {
      "name": "orbit_probe_stripo_inline_html",
      "description": "Diagnostic probe \u2014 empirically confirms that pushing locally-assembled HTML into a Stripo email is NOT supported by Stripo's API. All three inline-HTML paths (legacy `areas` shape, `dataSources` inline html, edit-after-push via PUT) fail \u2014 the first two silently. Tests each path in turn and verifies the sentinel HTML appears in the fetched rendered email. Auto-discovers the gen-area name from the master template (never hardcoded). Writes findings to <workspace>/outputs/stripo-inline-html-probe/<timestamp>.md. Kept for regression-detection. Not user setup guidance."
    },
    {
      "name": "orbit_learn_email_template",
      "description": "Parse an HTML email template (Stripo-aware) into modules + brand tokens and save to Orbit's library so future conversations can reference it. Use this the first time a user pastes an email template."
    },
    {
      "name": "orbit_build_email_from_template",
      "description": "Compose an on-brand email from a previously-learned template's modules. Output is Stripo-pasteable with es-* / esd-* structure intact."
    },
    {
      "name": "orbit_modify_email_template",
      "description": "Apply structured edits (remove module, change CTA/heading text, swap image) to an assembled email referencing a learned template's module catalog."
    },
    {
      "name": "orbit_check_email_auth",
      "description": "Resolve DNS records for a domain and verify SPF, DMARC, and DKIM (common selectors + any you supply). Returns pass/warn/fail with specific remediation strings."
    },
    {
      "name": "orbit_check_bimi",
      "description": "Validate a domain's BIMI record \u2014 SVG logo URL, VMC certificate, and DMARC-policy prerequisite check."
    },
    {
      "name": "orbit_dark_mode_check",
      "description": "Scan an HTML email for colour pairs that invert into invisible or low-contrast text in Apple Mail / Outlook dark mode."
    },
    {
      "name": "orbit_accessibility_lint",
      "description": "WCAG AA lint on email HTML \u2014 alt-text coverage, contrast ratios, semantic heading order, link-text quality, html lang, layout-table role=presentation."
    },
    {
      "name": "orbit_rfm_score",
      "description": "Score a customer list on RFM quintiles and classify each user into a named segment (Champions, Loyal, At Risk, Hibernating, \u2026) with per-segment revenue share and recommended actions."
    },
    {
      "name": "orbit_cohort_retention",
      "description": "Build retention curves from enrollment + revenue-event data. Returns per-cohort period-over-period retention% plus an aggregate curve."
    },
    {
      "name": "orbit_score_preheader",
      "description": "Score an email preheader: client-by-client inbox-preview clipping, duplicate-subject risk, greeking, placeholder leakage."
    },
    {
      "name": "orbit_audit_unsubscribe_page",
      "description": "Fetch and lint an unsubscribe URL for one-click compliance, password-gate, preference-centre granularity, and Gmail / Yahoo 2024 bulk-sender rules."
    },
    {
      "name": "orbit_validate_liquid",
      "description": "Syntax + fallback check on Braze Liquid \u2014 tag balance, default-filter coverage, unknown-attribute detection against a supplied inventory."
    },
    {
      "name": "orbit_compose_sms",
      "description": "SMS composer \u2014 detect GSM-7 vs UCS-2 encoding, count segments, inject regional compliance footer (US CTIA, AU, UK/EU GDPR, CA CASL)."
    },
    {
      "name": "orbit_free_shipping_threshold",
      "description": "Free-shipping threshold calculator. Given AOV, margin, and shipping cost, returns conservative/standard/aggressive thresholds with net-contribution math."
    },
    {
      "name": "orbit_replenishment_calc",
      "description": "Given pack size and daily consumption, compute the 3-touch replenishment email schedule (early nudge, reminder, last-chance)."
    },
    {
      "name": "orbit_build_exec_report",
      "description": "Turn raw channel stats into a Notion-ready exec-summary markdown with headline numbers, per-channel table, and narrative. Supports multi-currency via currency_code."
    },
    {
      "name": "orbit_qa_email",
      "description": "One-shot pre-send email QA gate \u2014 runs accessibility, dark-mode, and Gmail-clipping checks in a single call and returns a combined pass/warn/fail verdict."
    },
    {
      "name": "orbit_render_email_preview",
      "description": "Produce desktop, mobile, and dark-mode preview HTML artifacts from arbitrary email HTML."
    },
    {
      "name": "orbit_parse_postmaster_signal",
      "description": "Interpret Gmail Postmaster Tools data (CSV export or snapshot) with Gmail-threshold context and recommended actions per metric."
    },
    {
      "name": "orbit_list_growth_forecast",
      "description": "Project 12-month subscriber list trajectory from current size, acquisition, and churn. Returns trajectory table plus break-even / halving milestones."
    },
    {
      "name": "orbit_gdpr_consent_audit",
      "description": "Scan signup-page / email-footer / preference-centre HTML for GDPR consent signals \u2014 pre-ticked checkboxes, sender identifiability, purpose specificity, right to withdraw, privacy link."
    },
    {
      "name": "orbit_parse_test_readout",
      "description": "Turn A/B test counts into a written read-out with significance, 95% CI on rate difference, verdict (winner/loser/inconclusive), and ship-or-not recommendation."
    },
    {
      "name": "orbit_get_stripo_email",
      "description": "Fetch a single generated email from your Stripo workspace by numeric ID (GET /emails/<id>). Recovers the rendered copy/structure of an email pushed earlier \u2014 useful because slot_values are baked in server-side at push time and don't appear in local compose previews. Read-only."
    },
    {
      "name": "orbit_delete_stripo_email",
      "description": "Delete one or more generated emails from your Stripo workspace by numeric ID (DELETE /emails/<id> each). Accepts a single ID or an array; only the explicit IDs given are deleted (no delete-all). Returns a per-ID deleted/failed breakdown. Master templates are protected by the write-guard and cannot be touched."
    },
    {
      "name": "orbit_check_stripo_auth",
      "description": "Live-probe Stripo credentials before relying on them. Reports whether the REST token and Plugin credentials are configured and actively validates the REST token with a read-only GET, so an expired or revoked token (the 401 that blocks pushes) is caught proactively instead of mid-push."
    },
    {
      "name": "orbit_list_stripo_emails",
      "description": "List generated emails in your Stripo workspace (GET /emails) \u2014 paginated, searchable, sortable; returns id/name/folder_id/timestamps/editor_url/preview_url per email. folder_id filtering is RECURSIVE (includes subfolders). Read-only; max 100 per page."
    },
    {
      "name": "orbit_list_stripo_folders",
      "description": "Fetch the full recursive folder tree of your Stripo workspace (GET /folders/EMAIL or TEMPLATE). Read-only discovery \u2014 Stripo has NO folder write/move API; use the IDs for email-list filters and as destinations for the cabinet-UI move recipe."
    },
    {
      "name": "orbit_list_stripo_templates",
      "description": "List templates in your Stripo workspace (GET /templates) with IDs, names, editor/preview URLs. Auto-discovers the master template ID (is_configured_master flags the one Orbit pushes into). Read-only."
    },
    {
      "name": "orbit_import_stripo_template",
      "description": "Create a NEW Stripo template from an MJML document (POST /templates/import/mjml \u2014 Stripo's only API door into template creation). Reads the template back after import and checks the esd-email-gen-area marker + zero-padding rule; returns template_id, editor_url, preview_url + verification report (never compiled HTML). NOTE: mj-raw content is DROPPED by Stripo's compiler, so the gen-area marker always needs one manual editor step (the response's next_step explains it). Existing templates stay API-read-only. Counts 1 against the email_and_template quota."
    },
    {
      "name": "orbit_export_stripo_email_html",
      "description": "Export one generated Stripo email as compiled, CSS-inlined, send-ready HTML to a local file (returns path + byte count, never raw HTML). METERED against the org's export quota (~300/period) \u2014 check orbit_get_stripo_limits first and never loop it. Emails only (template export strips the gen-area marker)."
    },
    {
      "name": "orbit_get_stripo_limits",
      "description": "Read Stripo org quota counters: email_and_template (stored emails+templates vs plan cap \u2014 pushes fail at the cap), export (metered HTML-export quota), timer. Surfaces count/limit/remaining + warnings at 85% pressure. Check before push batches or exports."
    },
    {
      "name": "orbit_esp_capabilities",
      "description": "Return the honest capability matrix for one ESP or all of them \u2014 what each platform supports natively, what is partial (with the named constraint), and what its public API cannot do. Includes the reason and nearest alternative for every unsupported operation."
    },
    {
      "name": "orbit_check_esp_auth",
      "description": "Probe the configured credentials for an ESP (defaults to your ORBIT_DEFAULT_PLATFORM, or braze) and report ok, auth_failed, or needs_setup. Returns the exact setup message naming which fields are missing and where to find them."
    },
    {
      "name": "orbit_esp_templates",
      "description": "Read email templates from any supported ESP in a normalized shape (list or get a single template with HTML). Pass platform to target a specific ESP, or rely on the default-platform fallback."
    },
    {
      "name": "orbit_esp_push_template",
      "description": "Create or update an email template on a supported ESP (write path, kept separate from reads so approving a read never silently approves a write). Returns whether the template was created or updated, its id, and a link; returns an unsupported response where the ESP has no template CRUD (Customer.io)."
    },
    {
      "name": "orbit_esp_read",
      "description": "Read campaigns and flows, segments and lists, or performance metrics from any supported ESP, normalized with the untranslated payload attached. Stats an ESP cannot provide are left null and listed as unavailable, never zero-filled."
    },
    {
      "name": "orbit_esp_send_test",
      "description": "Send a proof email through an ESP that supports test sends, to a named recipient. Returns an unsupported response with the nearest alternative where the ESP has no test-send endpoint (Klaviyo \u2014 use render plus Orbit's local QA gate)."
    },
    {
      "name": "orbit_bootstrap_brain",
      "description": "Scaffold a ready template-brain repo at a given path \u2014 README with the four operating rules, CONVENTIONS, stage program folders, knowledge stubs, and a graphify-out gitignore \u2014 worded for the user's named company and ESP. Refuses to overwrite a non-empty directory."
    },
    {
      "name": "orbit_scaffold_brain_program",
      "description": "Create one program folder under a stage with a fully-framed prd.md stub (status backlog, human_approved false so AI drafts never build unreviewed) plus pre-cross-linked copy, email-build, and technical spec siblings. The stub is what makes the program exist to any agent reading the brain."
    },
    {
      "name": "orbit_init_verified_claims",
      "description": "Generate a verified-claims file with the staleness rule, the receipt table (claim, raw value, safe rounded-down display, source query, date read), and the hard gate that any figure in a proof module must trace to a receipt. Also writes a check-claims script that fails a build on figures absent from the claims file."
    },
    {
      "name": "orbit_generate_brain_gate",
      "description": "Generate a build gate script parameterised to the user's limits and templating branches \u2014 compile, resolve every branch off one variable map, byte-based clip check, mobile overflow, orphan and CTA-parity checks, axe-core, and master-module drift. States its honest scope: a layout and structure gate only; render and inbox truth stay with Orbit's render gate."
    }
  ],
  "keywords": [
    "lifecycle marketing",
    "crm",
    "martech",
    "retention",
    "growth marketing",
    "claude desktop",
    "mcp",
    "diagram generation",
    "brand-safe image generation",
    "email headers",
    "figma import",
    "mjml",
    "email html",
    "braze",
    "braze api",
    "email components",
    "notion export",
    "template library"
  ],
  "compatibility": {
    "claude_desktop": ">=1.0.0",
    "platforms": [
      "darwin",
      "win32",
      "linux"
    ],
    "runtimes": {
      "node": ">=20.0.0"
    }
  }
}
